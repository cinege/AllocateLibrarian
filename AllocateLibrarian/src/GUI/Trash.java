package GUI;

import org.eclipse.swt.SWT;
import org.eclipse.swt.events.SelectionAdapter;
import org.eclipse.swt.events.SelectionEvent;
import org.eclipse.swt.layout.GridData;
import org.eclipse.swt.widgets.Button;
import org.eclipse.swt.widgets.Combo;
import org.eclipse.swt.widgets.Label;

public class Trash {
/*
 * 
 * 
 * grds[i] = new GridData(SWT.UP, SWT.FILL, true, true);
			//grds[i] = new GridData(SWT.CENTER, SWT.CENTER, true, false, 1, 1);
			//grds[i].widthHint = 100;
			//grds[i].heightHint = 80;
			combo[i*2] = new Combo(shell, SWT.DROP_DOWN);

			//combo[i].setLayoutData(grds[i]);
			
			combo[i*2].setLayoutData(grds[i]);
			combo[i*2].setItems(employees);
			combo[i*2].addSelectionListener(new SelectionAdapter() {
				@Override
				public void widgetSelected(SelectionEvent e) {}
			});
			combo[i*2+1] = new Combo(shell, SWT.DROP_DOWN);
			combo[i*2+1].setLayoutData(grds[i]);
			combo[i*2+1].setItems(employees);
			combo[i*2+1].addSelectionListener(new SelectionAdapter() {
				@Override
				public void widgetSelected(SelectionEvent e) {}
			});
			
 * 
 * 
 * 
 * //grds[i] = new GridData(SWT.CENTER, SWT.CENTER, true, true, 1, 1);
			//grds[i].widthHint = 100;
			//grds[i].heightHint = 80;	
			l[i] = new Label(shell, SWT.CENTER);
			l[i].setLayoutData(new GridData(SWT.FILL, SWT.FILL, true, true));
			l[i].setText(stations[i]);
			
			
void singlecell(String type, String txt, boolean highlight) {
		GridData grd = new GridData(SWT.CENTER, SWT.CENTER, true, true, 1, 1);
		
		grd.widthHint = 110;
		grd.heightHint = 16;
		switch (type) {
		case "combo":
			Combo cmb = new Combo(shell, SWT.DROP_DOWN);
			cmb.setLayoutData(grd);
			cmb.setItems(txt);
			if (highlight) {cmb.setBackground(display.getSystemColor(SWT.COLOR_GRAY));}
			cmb.addSelectionListener(new SelectionAdapter() {
				@Override
				public void widgetSelected(SelectionEvent e) {}
			});
			break;
		case "label":
			Label l = new Label(shell, SWT.CENTER);
			l.setLayoutData(grd);
			l.setText(txt);
		break;
		case "button":
			Button btn = new Button(shell,SWT.BORDER);
			btn.setLayoutData(grd);
			btn.setText(txt);
			btn.addSelectionListener(new SelectionAdapter() {
				@Override
				public void widgetSelected(SelectionEvent e) {}
			});
		}
	}	
	
		    		// melyik sorban vagyunk --> dátum és idõpont --> stringgé alakítva
	    		int currentrow = i % this.columncount - 2;
	    		this.now.minusDays(dayoftheweek - 1).format(f)
	    		monthdayshift = String.valueOf(entry.month) + "-" + String.valueOf(entry.day) + String.valueOf(entry.shift);
	    		 
	    		String 
	    		this.schedule.monthoverview.get("").get(0).ordinal();
	
	
	
 */
}
